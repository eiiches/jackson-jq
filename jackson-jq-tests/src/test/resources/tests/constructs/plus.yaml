- q: 'error("foo") + empty'
  out: []
  comment: 'rhs is evaluated first'

- q: 'try (empty + error("foo")) catch .'
  out:
  - "foo"

- q: 'try (path(.[] + .[])) catch .'
  in: [1, 2]
  out:
  - "Invalid path expression with result 2"
