<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<artifactId>jackson-jq-core</artifactId>
	<name>${project.groupId}:${project.artifactId}</name>
	<packaging>bundle</packaging>

	<parent>
		<groupId>net.thisptr.jackson.jq.v2</groupId>
		<artifactId>jackson-jq-parent</artifactId>
		<version>2.0.0-SNAPSHOT</version>
	</parent>

	<dependencies>
		<dependency>
			<groupId>net.thisptr.jackson.jq.v2</groupId>
			<artifactId>jackson-jq-spi</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>net.thisptr.jackson.jq.v2</groupId>
			<artifactId>jackson-jq-json-provider</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>net.thisptr.jackson.jq.v2</groupId>
			<artifactId>jackson-jq-json-provider-impl-jackson2</artifactId>
			<version>${project.version}</version>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<profiles>
		<profile>
			<id>native</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.graalvm.buildtools</groupId>
						<artifactId>native-maven-plugin</artifactId>
						<version>1.1.8</version>
						<extensions>true</extensions>
						<executions>
							<execution>
								<id>test-native</id>
								<goals>
									<goal>generateTestResourceConfig</goal>
									<goal>test</goal>
								</goals>
								<phase>test</phase>
								<configuration>
									<resourceIncludedPatterns>
										<pattern>classpath_modules(/.*\.(jq|json))?</pattern>
										<pattern>tests(/.*\.ya?ml)?</pattern>
										<pattern>failing_tests(/.*\.ya?ml)?</pattern>
										<pattern>jq-test-extra-ok.json</pattern>
										<pattern>librocksdbjni-.*.(so|jnilib|dll)</pattern>
									</resourceIncludedPatterns>
								</configuration>
							</execution>
						</executions>
						<configuration>
							<fallback>false</fallback>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<executions>
					<execution>
						<id>compile-module-info</id>
						<goals>
							<goal>compile</goal>
						</goals>
						<configuration>
							<release>9</release>
							<compileSourceRoots>
								<compileSourceRoot>${project.basedir}/src/main/java9</compileSourceRoot>
							</compileSourceRoots>
							<multiReleaseOutput>true</multiReleaseOutput>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>javacc-maven-plugin</artifactId>
				<executions>
					<execution>
						<id>javacc</id>
						<goals>
							<goal>javacc</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-javadoc-plugin</artifactId>
				<configuration>
					<excludePackageNames>net.thisptr.jackson.jq.v2.internal.javacc</excludePackageNames>
					<sourcepath>${project.basedir}/src/main/java${path.separator}${project.build.directory}/generated-sources/javacc</sourcepath>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.felix</groupId>
				<artifactId>maven-bundle-plugin</artifactId>
				<executions>
					<execution>
						<id>generate-manifest</id>
						<phase>compile</phase>
						<goals>
							<goal>manifest</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<instructions>
						<Provide-Capability>
							osgi.serviceloader;osgi.serviceloader=net.thisptr.jackson.jq.v2.spi.Function;uses:="net.thisptr.jackson.jq.v2.spi",
							osgi.serviceloader;osgi.serviceloader=net.thisptr.jackson.jq.v2.spi.JqLibrary;uses:="net.thisptr.jackson.jq.v2.spi"
						</Provide-Capability>
						<Require-Capability>
							osgi.serviceloader;filter:="(osgi.serviceloader=net.thisptr.jackson.jq.v2.spi.Function)";cardinality:=multiple,
							osgi.serviceloader;filter:="(osgi.serviceloader=net.thisptr.jackson.jq.v2.spi.JqLibrary)";cardinality:=multiple,
							osgi.serviceloader;filter:="(osgi.serviceloader=net.thisptr.jackson.jq.v2.spi.module.Module)";cardinality:=multiple;resolution:=optional,
							osgi.extender;filter:="(&amp;(osgi.extender=osgi.serviceloader.processor)(version&gt;=1.0.0)(!(version&gt;=2.0.0)))",
							osgi.extender;filter:="(&amp;(osgi.extender=osgi.serviceloader.registrar)(version&gt;=1.0.0)(!(version&gt;=2.0.0)))"
						</Require-Capability>
						<Include-Resource>
							{maven-resources},
							META-INF/services/net.thisptr.jackson.jq.v2.spi.Function=${project.build.outputDirectory}/META-INF/services/net.thisptr.jackson.jq.v2.spi.Function,
							META-INF/services/net.thisptr.jackson.jq.v2.spi.JqLibrary=${project.build.outputDirectory}/META-INF/services/net.thisptr.jackson.jq.v2.spi.JqLibrary
						</Include-Resource>
					</instructions>
					<archive>
						<manifestEntries>
							<Multi-Release>true</Multi-Release>
						</manifestEntries>
					</archive>
				</configuration>
			</plugin>
		</plugins>
	</build>
</project>
